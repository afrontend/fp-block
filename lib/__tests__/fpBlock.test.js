const assert = require("assert");
const fpBlock = require("../index.js");

describe("fpBlock", () => {
  it("has a test", () => {
    assert(fpBlock !== undefined, "fpBlock should have a test");
  });
});
